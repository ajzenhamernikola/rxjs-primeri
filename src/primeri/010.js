import $ from 'jquery';
import * as rxjs from 'rxjs';
import {publish, take, map, mapTo, pluck} from 'rxjs/operators';
import { getSubscriber } from '../utils/getSubscriber';

export function main()
{
    
}